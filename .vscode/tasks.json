{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run node --watch index.js",
      "type": "shell",
      "command": "node --watch index.js",
      "problemMatcher": [],
      "isBackground": true,
      "presentation": {
        "reveal": "never", // Ensures terminal never opens
        "panel": "dedicated" // Runs in background (no UI)
      },
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
